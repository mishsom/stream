{
  "name": "server",
  "version": "1.0.0",
  "description": "server for stream application",
  "main": "index.js",
  "scripts": {
    "start": "json-server -p 3001 -w db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.14.2"
  }
}
